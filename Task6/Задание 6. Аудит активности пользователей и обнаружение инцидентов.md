Задание 6. Аудит активности пользователей и обнаружение инцидентов


# Инструкция запуска

## Шаг 1. Настройте среду Minikube с включённым audit-policy.yaml и экспортом лога (/var/log/audit.log).

```shell
minikube stop
minikube delete
```
```shell
mkdir -p ~/.minikube/files/etc/ssl/certs
cp "./audit-policy.yaml" ~/.minikube/files/etc/ssl/certs/audit-policy.yaml
```
```shell
minikube start --driver=docker --cpus=4 --memory=6g \
--extra-config=apiserver.audit-log-path=- \
--extra-config=apiserver.audit-policy-file=/etc/ssl/certs/audit-policy.yaml
```
```shell
minikube addons enable metrics-server
minikube addons enable default-storageclass
minikube addons enable storage-provisioner
```
```shell
minikube status
```

## Шаг 2. Запустите скрипт симуляции действий.

Проверить наличие файла
```shell
ls -l simulate-incident.sh\  
```
Задать права файлуу
```shell
chmod +x simulate-incident.sh\ 
```
Запустить симуляцию
```shell
./simulate-incident.sh\   
```

## Шаг 3. Проведите анализ audit.log.Шаг 3. Проведите анализ audit.log.

```shell
kubectl logs -f kube-apiserver-minikube -n kube-system > audit.log
```
```shell
chmod +x audit.py
./audit.py 
```
